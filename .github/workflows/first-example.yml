name: My First Workflow

on: push

jobs:
    first_job:
      name: job
      runs-on: ubuntu-latest

      steps:

      - name: welcome message
        run: echo "My first github actions job"
        
      - name: list files
        run: ls

      - name: Read files
        run: cat README.md

    